# Claude AI assistant
.claude/

# Go build artifacts (generated by go mod init)
# Dependency manifests are source, not build artifacts.
cmd/certificate-probe/build/
cmd/cert-backend-test*
cmd/cert-tests
cmd/https-probe.jar

# Local tun2proxy source checkout (build-tun2proxy.* clones to a temp dir;
# this only guards against a manual `git submodule add` / clone)
tun2proxy/

# Packaging output
*.zip

# OS files
Thumbs.db
.DS_Store
